# BTM 2026 conference app

Static, installable Progressive Web App for the Brain Tumor Meeting 2026 (programme, abstracts, venue and travel information).

Served with GitHub Pages from the `main` branch root. There is no build step. To release an update, replace the changed files and bump `VERSION` at the top of `service-worker.js` so installed clients refresh their cache.
